# Google Gemini API Key
# Get your key from: https://aistudio.google.com/app/apikey
# Replace 'your-api-key-here' with your actual API key
GEMINI_API_KEY=your-gemini-api-key-here

# Google Gemini Configuration for ADK Agent
# Model to use for Gemini agent (default: gemini-2.0-flash-exp)
# Options: gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash, etc.
GEMINI_MODEL=gemini-2.5-flash

# ADK Agent Configuration (used by ADK framework)
ADK_MODEL=gemini-2.5-flash

# Ollama Configuration for ADK Agent
# Model to use for Ollama agent (default: qwen3:14b)
# Options: qwen3:14b, gemma2:27b, llama3.1:70b, mistral:latest, etc.
# To test different models, change this value and restart the Ollama agent
OLLAMA_MODEL=qwen3:8b

# Ollama Base URL (default: http://localhost:11434/v1)
# Change this if Ollama is running on a different host/port
OLLAMA_BASE_URL=http://localhost:11434/v1

# Evaluation Configuration
# Controls which evaluator implementation to use (no fallback logic)

# Use Ollama (true) or Gemini (false) for evaluation judge
# - true: Use local Ollama models (slower, requires local server)
# - false: Use Gemini API (faster, requires API key)
USE_OLLAMA_EVAL=false

# Use Gemini for structured output (instead of Ollama)
USE_GEMINI_STRUCTURED_OUTPUT=true

# Use Real Ragas library (true) or Custom implementation (false)
# - true: Use official Ragas library (may have dependency conflicts)
# - false: Use custom Ragas-style evaluator (stable, working)
USE_REAL_RAGAS=true
